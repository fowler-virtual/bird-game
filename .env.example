# ローカルでデプロイ後、npm run deploy:seed が表示するアドレスをコピーして貼り付ける
# フロントで $SEED 残高取得に使う（Vite は VITE_ プレフィックスが必要）
VITE_SEED_TOKEN_ADDRESS=

# ゲームが $SEED を受け取るアドレス（ガチャ・Loft 支払い先）。ローカルではデプロイ元アカウントなど
VITE_SEED_TREASURY_ADDRESS=

# --- Claim API（ローカル server）用。.env にのみ設定し Git にコミットしない ---
# 報酬プールの秘密鍵。Claim 時はこのアドレスからユーザーへ transfer（新規ミントではない）。
# ローカルでは Hardhat Account #2 をプールとする想定。npm run accounts で Account #2 の秘密鍵をコピーして設定。
REWARD_POOL_PRIVATE_KEY=
# Claim 署名用の秘密鍵。ローカルでは上記 REWARD_POOL_PRIVATE_KEY と同じ値（Account #2）を設定する。
CLAIM_SIGNER_PRIVATE_KEY=
# SeedToken の owner（Hardhat Account #0）。プール入金（npm run fund-pool）で mint を実行するのに使用。
OWNER_PRIVATE_KEY=
# 例: http://127.0.0.1:8545
RPC_URL=
# SeedToken コントラクトアドレス（VITE_SEED_TOKEN_ADDRESS と同じで可）
SEED_TOKEN_ADDRESS=

# フロントから Claim API を呼ぶ URL。ローカル: http://localhost:3001
VITE_CLAIM_API_URL=

# ローカルで Claim するときはフロントのオリジンを指定（未設定だと * になり Cookie が送られず 401 になる）
# カンマ区切りで複数指定可（ポート可変対応）。例: http://localhost:5174,http://localhost:5175
ALLOWED_CLAIM_ORIGIN=

# セッション Cookie 用の秘密（16 文字以上必須）。ローカルでは任意の文字列で可。
SESSION_SECRET=

# Claim API が EIP-712 署名に使うチェーン ID。ローカル Hardhat は 31337
REWARD_CLAIM_CHAIN_ID=
# Claim API が EIP-712 の verifyingContract に使うアドレス（REWARD_CLAIM_ADDRESS と同じで可）
REWARD_CLAIM_CONTRACT_ADDRESS=

# RewardClaim コントラクトアドレス（npm run deploy-reward-claim でデプロイ後、表示されるアドレスを設定）
# フロントで claim トランザクション送信に必要（ガス代はユーザー負担）
VITE_REWARD_CLAIM_ADDRESS=
# 上と同じアドレス。approve スクリプト（npm run approve-claim）用
REWARD_CLAIM_ADDRESS=

# （任意）Claim API のポート。未設定時は 3001
# CLAIM_API_PORT=3001

# --- Sepolia 用（テストネットにデプロイするとき） ---
# Hardhat の sepolia ネットワークで使う RPC とデプロイヤ鍵
SEPOLIA_RPC_URL=
SEPOLIA_DEPLOYER_KEY=

# Sepolia 上にデプロイしたコントラクトアドレス（GitHub Pages / 本番フロント用）
# VITE_SEED_TOKEN_ADDRESS=0x...
# VITE_NETWORK_STATE_ADDRESS=0x...
# VITE_REWARD_CLAIM_ADDRESS=0x...

# Claim API 側で使う Sepolia 向け設定
# RPC_URL=https://sepolia.infura.io/v3/...
# SEED_TOKEN_ADDRESS=0x...
# REWARD_CLAIM_ADDRESS=0x...
